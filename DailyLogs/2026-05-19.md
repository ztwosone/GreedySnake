# 2026-05-19 开发日志

## 完成内容

### 严格测试门禁试运行

- 将 `Project/Test/test_runner.gd` 延后一帧执行测试，避免在场景树 `_ready()` 阶段向 root 添加测试节点导致 Godot 报 `Parent node is busy setting up children`。
- 为测试 runner 增加退出前 cleanup flush，给 `queue_free()` 和渲染资源清理留出帧。
- 修复 `VFXManager.screen_shake()` 在测试环境未挂载 GameWorld 时访问空父节点的问题。
- 更新 `Tools/run_tests_strict.ps1` allowlist，仅豁免负向 AtomRegistry 测试和 Godot headless 退出期资源日志。

## 验证结果

- 普通测试：`1614/1614` 断言通过。
- 严格测试：`STRICT PASSED: no unallowed Godot error output detected.`

## 下一步

- Orchestrator 可继续派发 `001-l3-run-loop` 的 `T001`。

## L3 Foundation T001-T004

- 新增 `Project/Test/cases/test_l3_run_loop.gd`，先 Red 验证 L3 配置 section、ConfigManager 访问器和 EventBus 信号缺失。
- `game_config.json` 新增最小 L3 v1 配置：run、floor、room_types、rewards、endpoint。
- `ConfigManager` 新增 L3 配置访问 API。
- `EventBus` 新增 L3 run/room/reward/floor/victory 信号。
- 验证：`1666/1666` 断言通过，严格测试通过。

## L3 Foundation T005-T008

- 扩展 `Project/Test/cases/test_l3_run_loop.gd`，先 Red 覆盖 RunState 生命周期和 FloorMap 生成。
- 新增 `Project/systems/run/run_progression_system.gd`，支持 run start、victory、death、cleanup、当前房间和可进入房间查询。
- 新增 `Project/systems/rooms/floor_map_generator.gd`，从 JSON `floor.fixed_v1_path` 生成确定性 v1 房间路径。
- 严格测试 runner 改为要求启动前设置 `GODOT_DISABLE_CRASH_HANDLER=1`，避免 Windows 原生崩溃弹窗干扰自动化。
- 验证：`1709/1709` 断言通过，严格测试通过。

## L3 US1 T009-T012

- 新增 `Project/Test/cases/test_l3_room_flow.gd`，先 Red 验证 combat `required_count`、RoomFlowSystem、RoomIntentPanel 和 GameWorld 接入缺失。
- `game_config.json` 为 L3 room_types 增加 JSON 驱动的 `required_count`。
- 新增 `Project/systems/rooms/room_flow_system.gd`，进入房间后监听既有 `enemy_killed` 事件推进 `clear_enemies` 目标，并单次发出完成事件。
- 新增 `Project/ui/room_intent_panel.gd`，以色块/文本占位展示房间意图、目标进度和完成状态。
- `game_world.tscn` / `game_world.gd` 接入 RunProgressionSystem、RoomFlowSystem 和 RoomIntentPanel。
- 验证：Red 阶段 7 个预期失败；Green 后 `1732/1732` 断言通过，严格测试通过。

## L3 US2 T013-T016

- 先更新 SpecKit plan/data-model/quickstart，明确奖励触发来源由 JSON 配置，v1 从 reward room entry 触发。
- 新增 `Project/Test/cases/test_l3_rewards.gd`，先 Red 验证 reward trigger 配置、RewardFlowSystem、RewardChoicePanel 和 GameWorld 接入缺失。
- `game_config.json` 为 rewards 增加 `trigger_room_types: ["reward"]`。
- 新增 `Project/systems/rewards/reward_flow_system.gd`，生成最多 3 个可应用 Build 奖励，选择后接入既有 head/tail/scale 管理器。
- 新增 `Project/ui/reward_choice_panel.gd`，以按钮、色块和文本占位表达奖励选择与已选择反馈。
- `game_world.tscn` / `game_world.gd` 接入 RewardFlowSystem 和 RewardChoicePanel。
- 验证：Red 阶段 7 个预期失败；Green 后 `1756/1756` 断言通过，严格测试通过。

## L3 US1/US2 Reviewer Fix

- Reviewer 发现 L3 子节点强依赖会破坏继承的 L1/L2 验收场景；`game_world.gd` 改为 `get_node_or_null()`，并补测试让 L1/L2 acceptance scene 进树。
- 修正战斗房“清空敌人”提前完成：`combat.required_count=3` 与 `combat.enemy_count=3` 一致，并让敌人数量从 JSON 读取。
- 奖励触发从 combat completion 收回到 reward room entry，保持“每房间一个主要意图”。
- 修复真实生命周期问题：`EventLogPanel` 兼容 enemy/null 事件数据，`TickPulse` 退出树时断开信号和 tween，`game_world.cleanup()` 清理 TriggerManager 场景引用，`VFXManager` 在 VFX 层失效后重建。
- 验证：`1768/1768` 断言通过，严格测试通过。

## L3 US3 T017-T019

- 新增 `Project/Test/cases/test_l3_floor_progression.gd`，先 Red 验证固定路径房间推进、已完成/可进入房间状态、cleanup 清理和楼层进度 UI 缺失。
- `RunProgressionSystem` 增加房间完成监听、completed/available 状态更新、`advance_to_room()` 显式进入已解锁房间和进度摘要。
- 新增 `Project/ui/floor_progress_panel.gd`，以色块和文本占位展示当前固定路径进度、当前房间意图和完成状态。
- `game_world.tscn` 接入 `FloorProgressPanel`；UI 继续只作为功能占位，不阻塞玩法验证。
- 验证：Red 阶段 4 个预期失败；Green 后 `1787/1787` 断言通过，严格测试通过。

## L3 US3 Reviewer Fix

- Reviewer 发现 US3 只推进 run state，RoomFlow 未真正切换到下一房间；新增 Red 测试覆盖非当前房间 completion、RoomFlow 同步、rest/endpoint 自动完成和楼层 UI 完成反馈。
- `RunProgressionSystem` 现在忽略未解锁/非当前房间的 `room_completed`，completed 房间不再留在 enterable 列表。
- `RoomFlowSystem` 监听 `room_entered` 同步当前目标，并监听外部 `room_completed` 同步奖励房完成状态。
- `game_config.json` 为 `rest` / `endpoint` 增加 `auto_complete_on_enter: true`，保持 L3 v1 固定路径闭环且不引入额外概念。
- `FloorProgressPanel` 增加 completed/available 反馈并同步清理路径块。
- 验证：Red 阶段 9 个预期失败；Green 后 `1797/1797` 断言通过，严格测试通过。

## L3 US4 T020-T023A

- 新增 `Project/Test/cases/test_l3_run_end.gd`，先 Red 验证 endpoint victory、`floor_completed` / `run_victory`、snake death outcome 和 cleanup 停 Tick。
- `RunProgressionSystem` 在 endpoint 完成时发出 `floor_completed` 与 `run_victory`；监听 `snake_died` 将当前 run 标记为 death。
- `GameManager` 在 PLAYING 状态收到 `run_victory` 后复用 existing game over flow，cause 为 `victory`。
- `game_world.cleanup()` 补充停止 TickManager 和清空 GridWorld。
- `FloorProgressPanel` 增加 Next 请求，使用 `room_advance_requested` 经 RunProgressionSystem 进入下一房间。
- 新增 `Project/Test/cases/test_l3_smoke_run.gd`，用真实 GameWorld、RewardChoicePanel 和 FloorProgressPanel 跑完整固定路径到 victory。
- 验证：US4 Red 阶段 7 个预期失败；Next control Red 阶段 2 个预期失败；最终 `1831/1831` 断言通过，严格测试通过。
